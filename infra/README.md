# Infrastructure & Deployment

This directory contains infrastructure-as-code (IaC), container configurations (Docker / Compose / Kubernetes), and network orchestration scripts for the `example-dns` services:

- `example-dns.com` (Web interface & API)
- `example-dns.net` (Primary Nameserver)
- `example-dns.org` (Secondary Nameserver)

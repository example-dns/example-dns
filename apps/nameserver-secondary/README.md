# example-dns Secondary Nameserver (`example-dns.org`)

This directory houses the authoritative secondary nameserver software and service configuration for `example-dns.org`.

## Responsibilities

- Authoritative DNS fallback and geographic redundancy
- Automated zone synchronization from `example-dns.net` via AXFR/IXFR or secure API
- High availability resolution

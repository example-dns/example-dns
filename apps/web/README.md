# example-dns Web Interface (`example-dns.com`)

This directory houses the frontend web portal, user dashboard, and management API for `example-dns.com`.

## Responsibilities

- User authentication and domain management
- DNS record configuration and validation UI
- API gateway for DNS updates
